# Run-only subset of dataset_g1_locomotion.yaml, for training/validating the
# steering policy on running alone before merging walk back in.
motions:
  - file: "data/motions/g1/g1_run.pkl"
    weight: 1.0
